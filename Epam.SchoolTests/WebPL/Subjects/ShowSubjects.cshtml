@using WebPL.Models;
@using Entities;
@using Ioc;
@{
    if (!User.IsInRole("Admin"))
    {
        Response.SetStatus(HttpStatusCode.Forbidden);
    }
    Layout = "~/_Layout.cshtml";
    var subjects = DependencyResolver.SubjectLogic.GetAll();
}

    <div class="panel panel-default">
        <div class="panel-body">
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in subjects)
                    {
                        <tr>
                            <td style="width:35px;">
                                <p>@category.ID</p>
                            </td>
                            <td>
                                <p>@category.Name</p>
                            </td>
                            <td>
                                <a class="btn btn-outline-dark" role="button" href="~/Subjects/DeleteSubject?subjectId=@category.ID">Delete</a>
                            </td>
                            <td>
                                <a class="btn btn-outline-dark" role="button" href="~/Subjects/UpdateSubject?SubjectId=@category.ID">Edit</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

        </div>
    </div>